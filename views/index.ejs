<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>An toàn Giao thông</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar">
        <div class="nav-container">
            <div class="nav-logo">
                <span class="material-icons">traffic</span>
                <span>An toàn Giao thông</span>
            </div>
            <div class="nav-menu">
                <a href="/" class="nav-link active">
                    <span class="material-icons">home</span>
                    Trang chủ
                </a>
                <button id="quiz-btn" class="nav-link quiz-button">
                    <span class="material-icons">quiz</span>
                    Trả lời câu hỏi
                </button>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="main-content">
        <div class="container">
            <!-- Hero Section -->
            <section class="hero">
                <h1>Kiến thức An toàn Giao thông</h1>
                <p>Tìm hiểu về các biển báo giao thông và kiến thức an toàn khi tham gia giao thông</p>
            </section>

            <!-- Knowledge Sections -->
            <section class="knowledge-section">
                <h2>
                    <span class="material-icons">do_not_disturb</span>
                    Biển báo Cấm
                </h2>
                <div class="cards-grid">
                    <% bienBaoCam.forEach(item => { %>
                        <div class="knowledge-card">
                            <div class="card-image">
                                <img src="/<%= item.image %>" alt="<%= item.name %>" loading="lazy">
                            </div>
                            <div class="card-content">
                                <h3><%= item.name %></h3>
                                <% if (item.description && item.description.length > 0) { %>
                                    <div class="descriptions">
                                        <% item.description.forEach(desc => { %>
                                            <p><%= desc %></p>
                                        <% }); %>
                                    </div>
                                <% } else { %>
                                    <p class="no-description"></p>
                                <% } %>
                            </div>
                        </div>
                    <% }); %>
                </div>
            </section>

            <section class="knowledge-section">
                <h2>
                    <span class="material-icons">warning</span>
                    Biển báo Nguy hiểm
                </h2>
                <div class="cards-grid">
                    <% bienBaoNguyHiem.forEach(item => { %>
                        <div class="knowledge-card">
                            <div class="card-image">
                                <img src="/<%= item.image %>" alt="<%= item.name %>" loading="lazy">
                            </div>
                            <div class="card-content">
                                <h3><%= item.name %></h3>
                                <% if (item.description && item.description.length > 0) { %>
                                    <div class="descriptions">
                                        <% item.description.forEach(desc => { %>
                                            <p><%= desc %></p>
                                        <% }); %>
                                    </div>
                                <% } else { %>
                                    <p class="no-description"></p>
                                <% } %>
                            </div>
                        </div>
                    <% }); %>
                </div>
            </section>

            <section class="knowledge-section">
                <h2>
                    <span class="material-icons">directions</span>
                    Biển báo Hiệu lệnh
                </h2>
                <div class="cards-grid">
                    <% bienBaoHieuLenh.forEach(item => { %>
                        <div class="knowledge-card">
                            <div class="card-image">
                                <img src="/<%= item.image %>" alt="<%= item.name %>" loading="lazy">
                            </div>
                            <div class="card-content">
                                <h3><%= item.name %></h3>
                                <% if (item.description && item.description.length > 0) { %>
                                    <div class="descriptions">
                                        <% item.description.forEach(desc => { %>
                                            <p><%= desc %></p>
                                        <% }); %>
                                    </div>
                                <% } else { %>
                                    <p class="no-description"></p>
                                <% } %>
                            </div>
                        </div>
                    <% }); %>
                </div>
            </section>
        </div>
    </main>

    <!-- Quiz Modal -->
    <div id="quiz-modal" class="modal">
        <div class="modal-content">
            <div class="modal-header">
                <h2>Câu hỏi An toàn Giao thông</h2>
                <button id="close-modal" class="close-btn">
                    <span class="material-icons">close</span>
                </button>
            </div>
            <div class="modal-body">
                <div id="quiz-content">
                    <div class="loading">
                        <span class="material-icons rotating">hourglass_empty</span>
                        <p>Đang tải câu hỏi...</p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Result Modal -->
    <div id="result-modal" class="modal">
        <div class="modal-content">
            <div class="modal-header">
                <h2 id="result-title">Kết quả</h2>
            </div>
            <div class="modal-body">
                <div id="result-content"></div>
                <div class="modal-actions">
                    <button id="next-question-btn" class="btn btn-primary">
                        <span class="material-icons">arrow_forward</span>
                        Câu hỏi tiếp theo
                    </button>
                    <button id="close-result-btn" class="btn btn-secondary">
                        <span class="material-icons">close</span>
                        Đóng
                    </button>
                </div>
            </div>
        </div>
    </div>

    <footer class="footer">
        <div class="container">
            <p>&copy; 2025 Ứng dụng An toàn Giao thông. Giúp bạn học và thực hành kiến thức giao thông an toàn.</p>
        </div>
    </footer>

    <script src="/js/script.js"></script>
</body>
</html>
